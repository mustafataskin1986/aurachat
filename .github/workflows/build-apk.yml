name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Repositoriyi Çek
        uses: actions/checkout@v4

      - name: Node.js Kur
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Java JDK Kur
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Keystore Secret Var mı Kontrol Et
        id: checksecret
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Kayıtlı Keystore'u Secret'tan Yükle (SABİT SHA-1)
        if: steps.checksecret.outputs.exists == 'true'
        run: |
          mkdir -p ~/.android
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > ~/.android/debug.keystore
          echo "Kayıtlı keystore kullanılıyor, SHA-1 sabit kalacak."

      - name: Geçici Keystore Üret (secret henüz yok)
        if: steps.checksecret.outputs.exists == 'false'
        run: |
          echo "UYARI: ANDROID_KEYSTORE_BASE64 secret'ı henüz ayarlanmamış."
          rm -rf ~/.android/debug.keystore
          mkdir -p ~/.android
          keytool -genkeypair -alias androiddebugkey -keypass android -keystore ~/.android/debug.keystore -storepass android -dname "CN=Android Debug,O=Android,C=US" -keyalg RSA -keysize 2048 -validity 10000 -storetype PKCS12

      - name: Bağımlılıkları Yükle
        run: |
          if [ ! -f package.json ]; then
            npm init -y
          fi
          npm install --legacy-peer-deps \
            @capacitor/core@5.7.0 \
            @capacitor/cli@5.7.0 \
            @capacitor/android@5.7.0 \
            @capacitor/app@5.0.7 \
            @capacitor-community/contacts@5.0.5 \
            @codetrix-studio/capacitor-google-auth@3.3.0 \
            @capacitor/push-notifications@5.1.0 \
            @capacitor/camera@5.0.7 \
            @capacitor/filesystem@5.2.1 \
            @capacitor/share@5.0.7 \
            @capacitor/haptics@5.0.6

      - name: Web Dosyalarını Hazırla ve Konfigüre Et
        run: |
          rm -rf www
          mkdir -p www
          cp -r *.html *.js *.json *.css www/ 2>/dev/null || true
          cp -r js static assets css www/ 2>/dev/null || true
          cat << 'EOF' > capacitor.config.json
          {
            "appId": "com.aurachat.app",
            "appName": "AuraChat",
            "webDir": "www",
            "bundledWebRuntime": false,
            "server": {
              "url": "https://aurachat-amber.vercel.app",
              "cleartext": true
            },
            "plugins": {
              "GoogleAuth": {
                "scopes": ["profile", "email"],
                "serverClientId": "447747395966-3shresouu36769er1b9656oec8vd13aa.apps.googleusercontent.com",
                "forceCodeForRefreshToken": true
              },
              "PushNotifications": {
                "presentationOptions": ["badge", "sound", "alert"]
              }
            }
          }
          EOF

      - name: Capacitor Android Ekle ve Senkronize Et
        run: |
          npx cap add android || true
          npx cap copy android
          npx cap sync android

      - name: MainActivity.java - WebView Önbellek Ayarı Ekle
        run: |
          MAIN_ACTIVITY=$(find android -name "MainActivity.java" | head -n 1)
          if [ -n "$MAIN_ACTIVITY" ]; then
            cat > "$MAIN_ACTIVITY" << 'EOF'
          package com.aurachat.app;

          import android.os.Bundle;
          import android.webkit.WebSettings;
          import com.getcapacitor.BridgeActivity;

          public class MainActivity extends BridgeActivity {
              @Override
              public void onCreate(Bundle savedInstanceState) {
                  super.onCreate(savedInstanceState);
                  WebSettings settings = this.bridge.getWebView().getSettings();
                  settings.setCacheMode(WebSettings.LOAD_DEFAULT);
              }
          }
          EOF
            echo "MainActivity.java güncellendi."
          else
            echo "UYARI: MainActivity.java bulunamadı!"
          fi

      - name: Firebase google-services.json Dosyasını Ekle
        run: |
          if [ -n "${{ secrets.GOOGLE_SERVICES_JSON }}" ]; then
            echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > android/app/google-services.json
            echo "google-services.json başarıyla oluşturuldu."
          else
            echo "UYARI: GOOGLE_SERVICES_JSON secret'ı bulunamadı!"
          fi

      - name: Android İzinlerini ve Gradle Bağımlılıklarını Düzenle
        run: |
          MANIFEST_PATH="android/app/src/main/AndroidManifest.xml"
          STRINGS_PATH="android/app/src/main/res/values/strings.xml"
          BUILD_GRADLE="android/build.gradle"
          APP_BUILD_GRADLE="android/app/build.gradle"

          # Manifest İzinleri
          if [ -f "$MANIFEST_PATH" ]; then
            if grep -q 'android:allowBackup="true"' "$MANIFEST_PATH"; then
              sed -i 's/android:allowBackup="true"/android:allowBackup="false"/' "$MANIFEST_PATH"
            elif ! grep -q 'android:allowBackup' "$MANIFEST_PATH"; then
              sed -i 's/<application/<application android:allowBackup="false"/' "$MANIFEST_PATH"
            fi
            add_perm() {
              if ! grep -q "$1" "$MANIFEST_PATH"; then
                sed -i "/<\/manifest>/i \    <uses-permission android:name=\"$1\" \/>" "$MANIFEST_PATH"
              fi
            }
            add_perm "android.permission.READ_CONTACTS"
            add_perm "android.permission.WRITE_CONTACTS"
            add_perm "android.permission.POST_NOTIFICATIONS"
            add_perm "android.permission.CAMERA"
            add_perm "android.permission.RECORD_AUDIO"
            add_perm "android.permission.MODIFY_AUDIO_SETTINGS"
            add_perm "android.permission.READ_MEDIA_IMAGES"
            add_perm "android.permission.READ_MEDIA_VIDEO"
            add_perm "android.permission.READ_EXTERNAL_STORAGE"
            add_perm "android.permission.WRITE_EXTERNAL_STORAGE"
            add_perm "android.permission.VIBRATE"
            add_perm "android.permission.ACCESS_FINE_LOCATION"
            add_perm "android.permission.ACCESS_COARSE_LOCATION"
          fi

          # Google Services Gradle eklentileri
          if [ -f "$BUILD_GRADLE" ]; then
            if ! grep -q "com.google.gms:google-services" "$BUILD_GRADLE"; then
              sed -i 's/dependencies {/dependencies {\n        classpath "com.google.gms:google-services:4.4.0"/' "$BUILD_GRADLE"
            fi
          fi

          if [ -f "$APP_BUILD_GRADLE" ]; then
            if ! grep -q "com.google.gms.google-services" "$APP_BUILD_GRADLE"; then
              echo 'apply plugin: "com.google.gms.google-services"' >> "$APP_BUILD_GRADLE"
            fi
          fi

          if [ -f "$STRINGS_PATH" ]; then
            if ! grep -q "server_client_id" "$STRINGS_PATH"; then
              sed -i 's/<\/resources>/    <string name="server_client_id">447747395966-3shresouu36769er1b9656oec8vd13aa.apps.googleusercontent.com<\/string>\n<\/resources>/' "$STRINGS_PATH"
            fi
          fi

      - name: APK Derle (Gradle)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: APK Dosyasını Çıktı Olarak Kaydet
        uses: actions/upload-artifact@v4
        with:
          name: AuraChat-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk